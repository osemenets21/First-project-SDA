window.addEventListener('load', function() {
    const animatedElements = document.querySelectorAll('.header-logo');
    animatedElements.forEach(function(element) {
      element.classList.add('header-logo');
    });
  });
